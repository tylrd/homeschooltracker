# Required for Drizzle + Better Auth
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/homeschooltracker

# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=replace-with-a-long-random-secret-at-least-32-chars

# Must match the origin you open in the browser during local dev
BETTER_AUTH_BASE_URL=http://localhost:3000
